# bootcamp-project-eda

## AI BootCamp EDA project repository (shockz)

- Jupyter Notebook files
- Python modules

## Memos

```bash
$ pip list --format=freeze --exclude=pywin32 > requirements.txt
```
